<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ulrich.gode_game">

    <!-- Permissions nécessaires -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Déclaration des requêtes pour traiter du texte -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="gode_game"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/LaunchTheme">

        <!-- Activité principale de l'application -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Spécifie le thème de l'application pour l'écran de lancement -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
            />

            <!-- Intention pour démarrer l'application -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Meta-données Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Déclaration des services Firebase (Firebase Cloud Messaging, Authentication, etc.) -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false"/>

        <service
            android:name="com.google.firebase.auth.FirebaseAuthService"
            android:exported="false"/>

        <!-- Déclaration pour Google Sign-In -->
        <activity
            android:name="com.google.android.gms.auth.api.signin.GoogleSignInActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>
    </application>

</manifest>
